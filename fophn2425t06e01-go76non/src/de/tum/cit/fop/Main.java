package de.tum.cit.fop;

import java.util.Iterator;

public class Main {
    public static void main(String[] args) {
        // Uncomment the following code, to test your implementation (.◜ᴗ◝.)
        /*Parcel parcel1 = new Parcel(1);
        Parcel parcel2 = new Parcel(2);
        Parcel parcel3 = new Parcel(3);
        AirPostalService airPostalService = new AirPostalService(new Parcel[]{parcel1, parcel2, parcel3});

        Iterator<Parcel> iterator = airPostalService.iterator();

        System.out.println("Iterating through parcels:");
        while (iterator.hasNext()) {
            Parcel parcel = iterator.next();
            System.out.println(parcel.toString()); // Don't forget to uncomment the toString() method
        }

        String workerId = RandomHelper.generateRandomID();
        airPostalService.addNewWorker(new Worker(workerId, "Paula"));
        System.out.println(airPostalService.getWorkerById(workerId));*/
    }
}
